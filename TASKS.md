# TASKS.md — 카노트 개선 작업 (2026-08)

CLAUDE.md를 먼저 읽을 것. 특히 "하지 않는 것" 섹션과 "세율 데이터 규칙"(상수는 `/data/` 분리)은 이 작업 전체에 적용된다.
작업은 P0 → P1 → P2 순서로 진행하고, 한 작업이 끝날 때마다 커밋한다.

---

## P0 — 수익 구조와 애드센스 전제조건

### 1. .ics 캘린더 내보내기 (최우선)

- 목적: 웹 푸시가 사실상 불가능하므로, 알림을 사용자 폰 캘린더로 대체한다. "교체 시기 알림 → 재방문" 경로의 핵심.
- 구현:
  - 클라이언트 사이드에서 .ics 생성 (Blob + a[download], 서버·라이브러리 의존 최소화)
  - 대상 일정: ① 각 소모품 교체 예정일 ② 자동차 검사 만료일 ③ 자동차세 연납 신청 시작(매년 1/16, RRULE:FREQ=YEARLY)
  - 각 VEVENT에 VALARM 2개: 7일 전, 당일 오전 9시
  - SUMMARY 예: "카노트 — 엔진오일 교체 시기", DESCRIPTION에 사이트 URL 포함 (URL은 config 상수로 분리 — 커스텀 도메인 확정 시 한 곳만 수정)
  - 버튼: 항목별 "캘린더에 추가" + 전체 일정 일괄 내보내기
- 완료 기준: 생성된 .ics를 iOS 기본 캘린더와 Google 캘린더에서 열었을 때 일정·알림이 정상 등록된다.

### 2. 세금 페이지 → 수첩 프리필 등록

- 목적: 세금 페이지 방문자를 수첩 사용자로 전환 (핵심 전환 지표).
- 구현:
  - 각 차종 페이지에 "이 차로 수첩 시작하기" 버튼. 페이지 내 연식 드롭다운 선택값을 반영해 `/index.html?model={slug}&cc={cc}&fuel={fuel}&year={year}` 형태로 이동
  - 수첩: 해당 파라미터가 있으면 차량 등록 폼을 프리필 상태로 열고, 등록 완료 후 history.replaceState로 파라미터 제거
  - 이미 등록된 차가 있으면 덮어쓰지 말고 "추가 등록" 확인을 받는다
- 완료 기준: 아반떼 페이지에서 2022년 선택 → 버튼 → 폼에 차종·배기량·연식이 채워진 채 등록 가능.

### 3. 개인정보처리방침 · 이용약관 페이지

- 목적: 애드센스 심사 필수.
- 구현: `/privacy.html`, `/terms.html` 생성, 모든 페이지 푸터에 링크.
  - 골자: 모든 기록은 브라우저 로컬(localStorage)에만 저장되며 서버로 전송하지 않음 / 향후 광고(구글 애드센스) 및 제휴 링크(쿠팡파트너스) 도입 시 쿠키가 사용될 수 있음 / 계산 결과는 참고용이라는 면책
- 완료 기준: 두 페이지가 존재하고 전 페이지 푸터에서 접근 가능.

### 4. sitemap.xml + robots.txt

- 목적: 서치콘솔 등록 준비.
- 구현: 빌드(페이지 생성) 과정에서 전체 페이지 목록으로 sitemap.xml 자동 생성. robots.txt에 sitemap 경로 명시. 차종 페이지가 하나 늘면 sitemap에도 자동 반영되어야 한다.
- 완료 기준: sitemap.xml에 세금 페이지 전체 + 계산기 + 수첩 + 법적 페이지가 포함된다.

---

## P1 — 유입 표면적 확대

### 5. 범용 자동차세 계산기 페이지

- 목적: "자동차세 계산기" 머리 키워드 수용 + 미등재 차종 사용자 받이.
- 구현:
  - `/tax/calculator.html` 신설. 입력: 배기량(cc), 등록 연도, 전기차 토글. 출력: 연세액·연납액·연식별 표 (기존 차종 페이지의 계산 로직·표 템플릿 재사용)
  - 허브 페이지의 "찾는 차종이 없나요?" 안내가 현재 TCO로 가는데, 이 계산기로 링크를 바꾼다
- 완료 기준: 1,998cc / 2020년 입력 시 차종 페이지와 동일한 로직·절사 규칙으로 계산된다.

### 6. 구형 차종 데이터 확대 (약 20종)

- 목적: 차령 경감 때문에 세금 검색 수요는 구형 차주가 더 크다. 경쟁 페이지도 적다.
- 구현: `data/vehicles.json`에 행 추가만으로 페이지가 생성되어야 한다 (안 되면 이번에 그렇게 리팩터).
  - 후보: 그랜저 HG·IG, 쏘나타 YF·LF, 쏘나타 LPG, 아반떼 MD·AD, K5 1세대, K5 LPG, K7, 스포티지R, 쏘렌토R, 투싼 ix, 모닝 구형, 스파크 구형, 올란도, 모하비, 카니발 그랜드·올뉴, 티볼리 초기형
- **주의: 배기량 수치를 추정으로 넣지 말 것.** 확실하지 않은 차종은 cc 값을 `null` + `TODO` 주석으로 남기고 사용자에게 확인 목록을 출력한다. 잘못된 배기량은 잘못된 세액으로 직결된다.
- 완료 기준: 확인된 차종의 페이지가 생성되고 허브·sitemap에 자동 반영. 미확인 목록이 별도 출력된다.

### 7. 세율 기준일 표기

- 목적: 세금 정보의 신뢰 표시.
- 구현: `data/tax-rates.json`에 `lastVerified` 필드(예: "2026-08") 추가, 모든 세금 페이지·계산기 하단에 "2026년 세율 기준 · 최종 확인 {lastVerified}" 자동 출력.
- 완료 기준: 값 한 곳 수정으로 전 페이지 표기가 바뀐다.

### 8. 메인 페이지 정적 소개

- 목적: JS 로딩 전 "불러오는 중"만 보이는 문제. 첫 방문자·크롤러·애드센스 심사 대응.
- 구현: 앱 영역 아래(또는 로딩 전 표시 영역)에 정적 HTML로 서비스 소개 2~3문단 — 카노트가 무엇인지, 기록이 브라우저에만 저장된다는 점, 세금 계산·유지비 비교로 가는 링크. noscript에도 동일 안내.
- 완료 기준: JS를 끄고 접속해도 서비스가 무엇인지 읽을 수 있다.

---

## P2 — 확산·품질

### 9. JSON-LD 구조화 데이터

- 구현: 차종 페이지 템플릿에 FAQPage(2~3문항: "OO 자동차세는 얼마인가요?"—신차 기준 세액 답변, "1월 연납 할인은?", "차령 경감은 언제부터?")와 BreadcrumbList 스키마를 빌드 시 자동 생성.
- 완료 기준: 구글 리치 결과 테스트 통과.

### 10. 연식별 표 이미지 저장

- 목적: 커뮤니티·단톡방 공유 유도.
- 구현: canvas로 연식별 세액 표를 렌더 → "표 이미지로 저장" 버튼으로 PNG 다운로드. 이미지 하단에 사이트 주소 워터마크 (config의 URL 상수 사용).
- 완료 기준: 모바일 Safari·Chrome에서 저장 동작.

### 11. 인앱 브라우저 안내 배너

- 목적: 카톡·네이버앱 인앱 브라우저의 localStorage 격리로 기록이 사라져 보이는 문제 완화.
- 구현: UA에 KAKAOTALK·NAVER·Instagram·FBAV 등이 포함되면 수첩 화면 상단에 배너 — "카카오톡 브라우저에서는 기록이 따로 저장돼요. Chrome/Safari로 열면 기록이 유지됩니다." 닫기 버튼(세션 동안 재표시 안 함). 안드로이드는 intent:// 로 Chrome 열기 시도, iOS는 안내만.
- 완료 기준: 일반 브라우저에서는 배너가 보이지 않는다.

### 12. 쿠팡파트너스 슬롯 (코드 준비만)

- 구현: 소모품 항목 상세에 제휴 링크 컴포넌트 + 의무 고지 문구("이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다") 컴포넌트를 만들되, 실제 링크는 `data/affiliate.json`에서 읽는다. 링크가 비어 있으면 슬롯 자체를 렌더하지 않는다.
- 완료 기준: affiliate.json에 링크를 넣는 것만으로 노출된다. **링크 값 자체는 채우지 말 것 (가입 후 사용자가 채움).**

---

## 이 문서 범위 밖 (Claude Code가 하지 않는다)

- 도메인 구매·GitHub Pages 커스텀 도메인 연결
- 서치콘솔·네이버 서치어드바이저 등록, 애드센스·쿠팡파트너스 가입
- 손으로 쓰는 콘텐츠 글(연납 신청 가이드 등) — 초안 요청이 오면 돕되 스크린샷·실경험은 사용자 몫
- 커뮤니티 공유

## 전 작업 공통 규칙

- 세율·URL 등 상수 하드코딩 금지 — `/data/` 또는 config로
- CLAUDE.md "하지 않는 것" 목록의 기능을 제안·구현하지 않는다
- 모바일 퍼스트, 무거운 리소스 추가 금지 (Core Web Vitals)
- 작업 단위 커밋, 커밋 메시지에 작업 번호 표기 (예: `feat: #1 ics export`)
